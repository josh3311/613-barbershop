ANTHROPIC_API_KEY=
PORT=8080
# Unsplash access key (same value you may use as EXPO_PUBLIC_UNSPLASH_KEY in the Expo app)
UNSPLASH_ACCESS_KEY=

# Virtual try-on (POST /api/try-on) is stubbed — returns { "error": "Feature coming soon" }. No keys required.
# LIGHTX_API_KEY= (optional / legacy)
# IMGBB_API_KEY= (optional / legacy)

# Replicate API token — used for FLUX Kontext Pro hair try-on feature
REPLICATE_API_TOKEN=
# REPLICATE_PHOTOMAKER_VERSION=
